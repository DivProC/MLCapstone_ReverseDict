# Core (required)
fastapi>=0.110
uvicorn[standard]>=0.29
pandas>=2.0
numpy>=1.24
scikit-learn>=1.3
pydantic>=2.0
sentence-transformers>=2.6

# Optional -- only needed if you pick BERT [CLS] in the dropdown.
# Uncomment / install as needed:
# torch>=2.1
# transformers>=4.38
